import React from "react"


export default function Message(props) {

return(
<>

</>

// return (
//   <ul>
//     {props.messages && propsmessages.map((message) => <Message message={message} />)}
//   </ul>
// )
)
}