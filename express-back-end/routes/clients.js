const router = require("express").Router();

//all the clients routes comes here//

const clientRoutes = () => {
  return router;
};
const clientRoutes = () => {
  return router;
es = () => {
  return router;
};

module.exports = {
  clientRoutes,
  clientRoutes,
  clientRoutes,
};
