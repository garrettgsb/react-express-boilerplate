const router = require("express").Router();

// READ GET /users/:id  "Home Screen (Logged in User"

module.exports = (db) => {
router.get("", (req, res) => {
  db.query(``)
  .then()
  .catch()
})
return router;
}

